# Dependencies for the point-in-time factor research platform (src/).
#
# Deliberately SEPARATE from requirements.txt, which pins the intraday board's
# Streamlit Cloud image. The two products share a repo, not a runtime — adding
# platform deps to that file would force a Streamlit Cloud rebuild for changes
# it does not care about.

# core numerics
pandas>=2.2.0
numpy>=1.26.0
scipy>=1.12.0
statsmodels>=0.14.2
pyarrow>=15.0.0

# rate tables and configuration are data, not code (§9)
pyyaml>=6.0

# fetching / parsing
requests>=2.32.0
lxml>=5.2.0
python-dateutil>=2.9.0

# auth (§5 Dhan token daemon: TOTP)
pyotp>=2.9.0

# dashboard (§16) — FastAPI serves the local web app; the desktop shell wraps
# it in a native window. Chosen over Streamlit because it freezes cleanly
# under PyInstaller.
fastapi>=0.111.0
uvicorn>=0.30.0
jinja2>=3.1.4

# tests
pytest>=8.0.0
